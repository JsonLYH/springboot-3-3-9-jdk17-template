rocketmq:
  # NameServer
  name-server: 10.10.10.11:9876
  # 默认的消息组
  producer:
    group: mebay-339-jdk17-template-group
    timeout: 4000
spring:
  data:
    redis: # Redis连接配置
      host: 10.10.10.101  # Redis主机地址
      port: 6379  # Redis端口号
      password: mebay_redis00.  # 访问Redis所需密码
      database: 0  # 使用的数据库编号
      lettuce: #Lettuce客户端配置
        pool: # 连接池配置
          max-active: 100  # 最大活跃连接数
          max-wait: -1  # 最大等待时间（-1表示无限等待）
          max-idle: 30  # 最大空闲连接数
          min-idle: 10  # 最小空闲连接数
      connect-timeout: 8000ms
  cloud:
    nacos:
      discovery:
        server-addr: http://10.10.10.13:8848
        register-enabled: true
      config:
        server-addr: http://10.10.10.13:8848
        file-extension: yaml
  datasource:
    url: jdbc:mysql://10.10.10.13:4408/ai?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useInformationSchema=false&rewriteBatchedStatements=true
    username: root
    password: mebayMySQLPassword@..
    druid:
      initial-size: 5 #连接池初始化大小
      min-idle: 10 #最小空闲连接数
      max-active: 100 #最大连接数
      web-stat-filter:
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*" #不统计这些请求数据
      stat-view-servlet: #访问监控网页的登录用户名和密码
        login-username: druid
        login-password: druid
      keep-alive: true
jasypt:
  encryptor:
    password: mebay_pass_new